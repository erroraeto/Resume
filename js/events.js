import*as THREE from"https://cdn.jsdelivr.net/npm/three@0.179.1/+esm";import{OrbitControls}from"https://unpkg.com/three@0.112/examples/jsm/controls/OrbitControls.js";import*as EffectComposer from"https://cdn.jsdelivr.net/npm/three-effectcomposer@0.0.1/index.min.js";let gl=document.querySelector("#gl"),composer=new EffectComposer(renderer),renderPass=new RenderPass(scene,camera),effect=(composer.addPass(renderPass),new ShaderPass(DOFShader));function animate(){requestAnimationFrame(animate),composer.render()}effect.uniforms.focus.value=10,effect.uniforms.aperture.value=.02,effect.uniforms.maxBlur.value=1,composer.addPass(effect),animate();