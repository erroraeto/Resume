import*as THREE from"https://cdn.jsdelivr.net/npm/three/build/three.module.js";let width=window.innerWidth,height=window.innerHeight,camera=new THREE.PerspectiveCamera(70,width/height,.01,10),scene=(camera.position.z=1,new THREE.Scene),geometry=new THREE.BoxGeometry(.2,.2,.2),material=new THREE.MeshNormalMaterial,mesh=new THREE.Mesh(geometry,material),renderer=(scene.add(mesh),new THREE.WebGLRenderer({antialias:!0}));function animate(e){mesh.rotation.x=e/2e3,mesh.rotation.y=e/1e3,renderer.render(scene,camera)}renderer.setSize(width,height),renderer.setAnimationLoop(animate),document.body.appendChild(renderer.domElement);